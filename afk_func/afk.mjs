let canvas;
let duration;
let active;

canvas = document.getElementById('canvas');
active = prompt('on active anti afk per milliseconds');
duration = prompt('afk duration using ms (milliseconds)');

if(active && duration) {

} else {
    console.log('canceled anti afk')
}
